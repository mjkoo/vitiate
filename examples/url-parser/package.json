{
  "name": "example-url-parser",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "fuzz": "VITIATE_FUZZ=1 vitest run --project fuzz",
    "fuzz:cli": "vitiate test/url-parser.fuzz.ts -max_total_time=30"
  },
  "dependencies": {
    "@vitiate/core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^7.3.1",
    "vitest": "^3.1.0"
  }
}
