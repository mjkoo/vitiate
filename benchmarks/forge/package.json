{
  "name": "@vitiate/bench-forge",
  "private": true,
  "type": "module",
  "description": "vitiate coverage/throughput benchmark for node-forge (pure-CommonJS ASN.1/X.509 decoder), instrumented via instrument.packages.",
  "scripts": {
    "bench": "node ../run-lib.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "node-forge": "^1.3.1"
  },
  "devDependencies": {
    "@types/node-forge": "^1.3.14",
    "@vitiate/core": "workspace:*",
    "typescript": "^6.0.3",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "vitiate": "workspace:*"
  }
}
