{
  "name": "@vitiate/bench-jpeg",
  "private": true,
  "type": "module",
  "description": "vitiate coverage/throughput benchmark for jpeg-js (pure-CommonJS image decoder), instrumented via instrument.packages.",
  "scripts": {
    "bench": "node ../run-lib.mjs",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4"
  },
  "devDependencies": {
    "@vitiate/core": "workspace:*",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9",
    "vitiate": "workspace:*"
  }
}
